const mongoose = require('mongoose');
const commentRating = require('../models/commentRating.model');

module.exports = {
    getComment: (res, req, next) => {

    },
    insertComment: (res, req, next) => {

    },
    insertRating: (res, req, next) => {

    },
};
